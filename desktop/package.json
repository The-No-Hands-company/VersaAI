{
  "name": "versaai-desktop",
  "version": "0.1.0",
  "description": "VersaAI Desktop — chat, agents, and RAG in a native Tauri shell",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/dompurify": "^3.0.5",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "dompurify": "^3.3.2",
    "marked": "^17.0.4"
  }
}
